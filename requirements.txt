# Core Dependencies
httpx>=0.27.0          # Async HTTP client for API calls
requests>=2.31.0       # HTTP client (sync alternative)
sqlalchemy>=2.0.0      # ORM for database operations
pydantic>=2.5.0        # Data validation and settings
rapidfuzz>=3.0.0       # Fast fuzzy string matching

# Database
aiosqlite>=0.19.0      # Async SQLite support

# Utilities
python-dotenv>=1.0.0   # Environment variable management
rich>=13.7.0           # Beautiful terminal output
loguru>=0.7.2          # Better logging

# LLM Integration
groq>=1.0.0            # Groq API client (fast inference + Whisper)

# Voice Interface
faster-whisper>=1.0.0  # Speech-to-text (local Whisper)
sounddevice>=0.5.0     # Audio recording
soundfile>=0.12.1      # Audio file I/O (WAV support)
numpy>=1.26.0          # Audio processing
keyboard>=0.13.0       # Hotkey detection (push-to-talk)
pyttsx3>=2.90          # Text-to-speech (offline, fallback)
edge-tts>=6.1.0        # Microsoft Edge TTS (natural voices)
pygame>=2.5.0          # Audio playback for TTS

# Save file reader
spireslayer==2.0.0     # Read and parse Slay the Spire autosave files

# Development
pytest>=8.0.0          # Testing framework
pytest-asyncio>=0.23.0 # Async test support
black>=24.0.0          # Code formatter
ruff>=0.2.0            # Fast linter

# Future phases (commented out for now)
# chromadb>=0.4.0       # Vector database for RAG
